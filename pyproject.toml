[project]
name = "bookmarkvault-api"
version = "0.1.0"
description = "Backend API for managing bookmarks with tags. Built with FastAPI, SQLAlchemy, and PostgreSQL."
requires-python = ">=3.11"
dependencies = [
    "psycopg2-binary>=2.9.12",
    "sqlalchemy>=2.0.49",
]
